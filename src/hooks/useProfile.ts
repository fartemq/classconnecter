
import { useProfile } from "./profiles";
import type { Profile, ProfileUpdateParams } from "./profiles/types";

// Re-export the hook and types
export { useProfile };
export type { Profile, ProfileUpdateParams };
